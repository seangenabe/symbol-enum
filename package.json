{
  "name": "symbol-enum",
  "version": "3.1.0",
  "description": "Enum with symbols",
  "main": "lib/symbol-enum.js",
  "scripts": {
    "compile": "babel src -d lib --source-maps --optional runtime",
    "watch": "babel src -d lib --watch --source-maps --optional runtime",
    "test": "npm run compile && mocha",
    "prepublish": "npm run compile"
  },
  "bugs": "https://github.com/seangenabe/symbol-enum/issues",
  "repository": {
    "type": "git",
    "url": "http://github.com/seangenabe/symbol-enum.git"
  },
  "keywords": [
    "symbol",
    "enum"
  ],
  "author": "Sean Genabe <seangenabe@outlook.com>",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^2.1.0"
  },
  "dependencies": {
    "babel": "^5.8.20",
    "babel-runtime": "^5.8.20"
  }
}
