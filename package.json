{
  "name": "symbol-enum",
  "version": "3.1.0",
  "description": "Enum with symbols",
  "main": "lib/symbol-enum.js",
  "scripts": {
    "compile": "babel -d lib --source-maps --optional runtime src",
    "watch": "babel --watch -d lib --source-maps --optional runtime src",
    "pretest": "npm run compile",
    "test:compile": "babel -d test/lib --source-maps --optional runtime test/src",
    "test": "npm run test:compile && mocha test/lib",
    "prepublish": "npm run compile"
  },
  "bugs": "https://github.com/seangenabe/symbol-enum/issues",
  "repository": {
    "type": "git",
    "url": "http://github.com/seangenabe/symbol-enum.git"
  },
  "keywords": [
    "symbol",
    "enum"
  ],
  "author": "Sean Genabe <seangenabe@outlook.com>",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.8.21",
    "mocha": "^2.1.0"
  },
  "dependencies": {
    "babel-runtime": "^5.8.20"
  }
}
